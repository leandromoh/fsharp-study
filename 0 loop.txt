// expression is used to iterate in a loop over a range of values of a loop variable.
// the return type for the body-expression must be unit.

for identifier = start [ to | downto ] finish do
    body-expression

for identifier in enumerable-expression do
    body-expression

while test-expression do
    body-expression


  for i = 1 to 10 do
    printfn "%d" i

  for i in [ 1; 5; 100; 450; 788 ] do
    printfn "%d" i

  let mutable i = 0
  while i < 10 do
    printfn "%d " i
    i <- i + 1